#include <stdio.h>

/**
* main - Prints the alphabet in lowercase.
*
* Return: Always 0.
*/
int main(viod)
{
	char alphabet;
	for (alphabet = 'a', alphabet <= 'z', alphabet++);
{
	putchar('\n');
}
	return (0);
}

